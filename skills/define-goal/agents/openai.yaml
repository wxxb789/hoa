interface:
  display_name: "Define Goal"
  short_description: "Define one goal or a coherent goal set"
  default_prompt: "Use $define-goal to produce one cohesive goal or the smallest sufficient goal set, without planning implementation."
